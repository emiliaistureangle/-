# 《一箭又一箭》开发记录（含 AIGC 使用记录）

> 课程作业配套文档 ｜ 游戏本体：`1.py` ｜ 运行环境：Python 3.10.11 + pygame 2.6.1

---

## 一、如何运行与验证

| 命令 | 作用 |
| :--- | :--- |
| `python 1.py` | 正常开始游戏（600×800 窗口） |
| `python 1.py --solve` | 不加界面，校验全部关卡是否**必定可通关**，并打印一种通关顺序 |
| `python 1.py --test` | 不加界面，自测核心规则（路径检测、边界情况、胜负判定、状态机） |
| `python 1.py --help` | 查看文件头的完整说明 |

前置依赖：`pip install pygame`

操作方式：鼠标点击箭头即可；快捷键 `R` 重开本关、`ESC` 返回菜单、`空格` 确认。

---

## 二、AIGC 使用记录

使用的工具：**VS Code 内的 GitHub Copilot（对话式编程助手）**，采用"人给规则 → AI 写实现 → 人验证并纠错"的协作方式。下面按开发阶段如实记录。

### 1. 环境搭建

* **我输入的提示**：说明要做一个 Pygame 的网格解谜小游戏，窗口 600×800，需要能安装依赖并跑起来。
* **AI 输出**：给出 `pip install pygame` 的安装命令；随后检测发现本机未安装 pygame，由 AI 执行安装并确认版本为 2.6.1。
* **人工判断**：确认 pygame 比 Tkinter 更适合本作业的动画与事件处理需求（与技术文档 §1 的选型一致）。

### 2. 关卡设计与"是否死锁"的验证（本作业最关键的一步）

* **我输入的提示**：把本游戏的规则（点击箭头，若正前方到边界之间没有其它箭头则飞出消除，否则撞击）交给 AI，要求**写一个求解器，批量生成并筛选"可通关"的关卡**，且随难度递增。
* **AI 输出**：AI 先指出一个我原本没考虑到的结论 —— **规则本身允许出现"互锁死局"**，例如：

  ```
  →  0  ←        两个箭头互相阻挡，谁点都会撞，这一关永远无法清空
  ```

  因此关卡不能随手填。AI 据此写了生成 + 校验脚本：随机生成网格 → 用贪心求解器验证 → 用"依赖链深度"衡量难度 → 输出候选关卡。
* **关键算法说明（AI 给出的论证，我复核后采纳）**：贪心求解是**完备**的。因为"消除箭头"只会让其它箭头的射线更通畅（单调性），所以能拆就拆永不亏；一旦贪心卡住，就说明真的存在死锁。
* **AI 输出**：筛出 5 个递增难度关卡，并在代码里内置 `solve_level()` 供随时复核。
* **人工判断**：我从候选里手工挑掉了几关"只有唯一一步可走、过于劝退"的，把第 1 关改成自己设计的 3×3 热身关（初始有 2 支可点，依赖链只有 3 层）。

### 3. 核心逻辑与 UI 绘制

* **我输入的提示**：给出技术文档 §4.1 的路径检测流程（按方向设定起点、终点、步长），要求实现为 `blocker_of()`。
* **AI 输出**：把四段分支循环收敛成一段"从下一格开始，沿方向向量一直走到边界"的 `while` 循环，同时返回**挡路的那支箭头**（这样碰撞时可以让被撞的箭头一起闪一下，反馈更清楚）。
* **AI 输出**：棋盘虚线网格、箭头多边形、心形失误图标分别用 `draw_dashed_line`、`pygame.draw.polygon`、两个圆 + 一个三角拼出，**不依赖任何图片素材**，方便交作业时直接跑。

### 4. 动画与碰撞反馈

* **我输入的提示**：问 AI 在 Pygame 里怎么实现平滑移动、抖动、淡出。
* **AI 输出**：飞行动画用"偏移量按 `速度 × dt` 累加"而不是按帧累加；抖动用 `sin/cos` 随时间衰减；淡出用 `BLEND_RGBA_MULT` 对精灵做 alpha 乘法（并解释了为什么直接 `set_alpha` 对带透明通道的 Surface 无效）。
* **由此产生的设计决定**：把箭头的"逻辑状态 `removed`"和"表现状态 `state`"分开。点击瞬间就先把逻辑上消除掉，飞行动画只负责好看 —— 否则会出现"上一支箭还在飞的路上，别的箭头就被误判为被挡"的诡异 bug。

### 5. 开发过程中处理掉的问题（3 个实测故障 + 2 个写第一版时就规避的隐患）

| # | 现象 | 原因 | 处理 |
| :--- | :--- | :--- | :--- |
| 1 | 一画文字就崩溃：`TypeError: expected str, bytes or os.PathLike object, not int` | pygame 2.6.1 扫描 Windows 字体注册表时遇到非字符串项（`sysfont.py` 的 `initsysfonts_win32`），导致 `match_font` / `SysFont` 全部不可用 | 改为**直接按字体文件路径加载**（`C:\Windows\Fonts\msyh.ttc`），并保留"字体文件 → 系统字体库 → 内置默认字体"三级回退 |
| 2 | 底部提示文字被按钮压住 | 底部栏高度算少了，文字中心 y=710 与按钮顶部 y=708 重叠 | 重排底部栏：分隔线 → 提示文字 → 按钮，并留出底部边距 |
| 3 | 顶部"剩余箭头"数字和标签挤成一团 | 图标、数字、标签分别硬编码了 x 坐标 | 改成先测文字宽度再整体右对齐 |
| 4 | （隐患，未真的出现）卡顿时箭头可能"瞬移"穿过棋盘 | 若按帧累加位移，掉帧时一步会走太远 | 写第一版时就加单帧限幅：`dt = min(dt, 0.05)` |
| 5 | （隐患，未真的出现）浮层上的点击可能"穿透"到下面的棋盘 | 若把浮层按钮和棋盘点击写在同一个分支里，就会出现穿透 | 写第一版时就按状态分流事件：WIN/LOSE 状态下不处理棋盘点击 |

### 6. 验收（AI 辅助、但结论由我核对）

* `python 1.py --solve`：5 个关卡全部返回**可通关**，0 个死锁。
* `python 1.py --test`：**23 / 23 项通过**，覆盖：
  * 路径检测（同行阻挡、反方向不算阻挡、跨空格纵向扫描、到边界畅通）
  * 边界情况（1×1 棋盘、空网格）
  * 互锁死局能被求解器识别为无解
  * 点击 → 消除 → 清空 → 切换到胜利；连撞 3 次 → 切换到失败
  * 动画状态机（`COLLIDING` → `IDLE`）、重新开始后数据复位
* 离屏渲染抽查：逐状态截图检查 UI，并用像素取样确认"被点的箭头变红 `(255,91,91)`、被撞的箭头闪金 `(255,188,91)`"。

---

## 三、我做的事情 vs. AI 做的事情

| 环节 | 我（人） | AIGC |
| :--- | :--- | :--- |
| 需求与规则定义 | 提供技术开发文档，确定玩法、失误机制、界面布局 | — |
| 关卡数据 | 裁定难度曲线，选定最终的 5 个关卡 | 生成候选 + 判死锁 + 算难度 |
| 算法正确性 | 复核"贪心完备性"的论证 | 提出论证并实现 |
| 代码编写 | 给出算法描述与接口约定 | 生成绝大部分实现代码 |
| 故障发现与修复 | 确认修复方向、验收修复结果 | 跑出故障、分析堆栈、给出并应用修复 |
| 自动化验收 | 核对测试结论 | 编写并运行测试脚本（23 项断言 + 像素取样） |
| 试玩验收 | **待做**：亲自运行 `py -3.10 1.py` 试玩，确认手感与帧率 | — |

---

## 四、还可以继续做的扩展

1. 音效（用 `array` 合成方波，避免引入音频素材文件）。
2. 关卡编辑器（鼠标点格子生成关卡，再用内置求解器即时校验）。
3. 计时排行榜 / 最少失误记录（存档到 JSON）。
4. 难度自适应：按玩家失误率动态调整每关的失误机会。

---

## 五、具有代表性的 AIGC 使用过程（5 次）

> 借助的 AIGC 技术：**VS Code 内的 GitHub Copilot（对话式编程助手）**。
> 下表 5 条均出自本次真实开发过程，按发生时间先后排列，每一条都能对应到 `1.py` 里现存的代码，或对应到下面给出的验证命令。

| 子任务 | 借助何种 AIGC 技术 | AI 实现或提供了什么 | 效果如何 | 人工修改 |
| :--- | :--- | :--- | :--- | :--- |
| **关卡设计**（生成关卡数组） | GitHub Copilot（对话式） | 先指出本规则的关卡允许出现"互锁死局"，再写出「随机生成网格 + 贪心求解校验 + 用依赖链深度评估难度」的筛选脚本，产出 5 个递增难度关卡 | `--solve` 校验 5 关全部可通关、0 死锁；但候选里多数关卡"开局只有唯一一步可走"，对新手过于劝退 | 第 1 关不走生成脚本，改成手工写出的 3×3 布局（开局 2 支可点、依赖链 3 层）；从候选里挑掉过于刁钻的；要求把求解器 `solve_level()` 固化进游戏文件，便于随时复核 |
| **路径检测**（核心玩法） | 同上 | 把技术文档 §4.1 "按四个方向分别设置起点/终点/步长"的描述，收敛成一段沿方向向量 `while` 扫描到边界的 `blocker_of()`，并返回**挡路的那一支箭头**（而非只返回真假） | 一次写对。`--test` 中 6 项路径检测全部通过，含 1×1 棋盘、跨空格纵向扫描、反方向不算阻挡等边界情况 | 我提出把「逻辑状态 `removed`」与「表现状态 `state`」拆开：点击瞬间先改逻辑，动画只负责好看，否则会出现"前一支箭还在飞的路上，别的箭头就被误判为被挡" |
| **碰撞与飞行动画** | 同上 | 给出平滑移动、抖动、淡出的做法：位移按 `速度 × dt` 累加（不按帧）、抖动用 `sin/cos` 随时间衰减、淡出用 `BLEND_RGBA_MULT` 做 alpha 乘法（并说明为什么 `set_alpha` 对带透明通道的 Surface 无效） | 一次可用，没有返工：箭头按方向飞出并淡出，撞击有"抖动 + 变红 + 震屏 + 被撞者闪金"。离屏像素取样确认被点箭头为红 `(255,91,91)`、被撞箭头为金 `(255,188,91)`，飞行结束后 `state=GONE`、`alpha=0` | 未改动画参数（撞击 0.40s、抖动 6px、红闪 0.12s 都保留 AI 给出的取值）。验收标准上，我要求"要能一眼看出是谁挡了谁"，由 AI 一并给出的"被撞箭头闪金 + 飞行拖尾"满足，予以保留 |
| **中文字体崩溃排查** | 同上 | 依据 `TypeError: expected str, bytes or os.PathLike object, not int` 的堆栈，定位到 pygame 2.6.1 扫描 Windows 字体注册表的缺陷（`sysfont.py` 的 `initsysfonts_win32`），给出"直接按字体文件路径加载 + 三级回退"的修复 | 修复后中文标题、按钮、提示全部正常显示，离屏截图与自测均无异常 | 我把候选字体表补成"雅黑 → 黑体 → 等线 → 宋体 → Noto → 文泉驿 → 苹方"，并让粗体优先命中 `msyhbd.ttc`，避免合成加粗在中文笔画上发虚 |
| **界面自检与验收** | 同上 | 写出无窗口自测脚本（23 项断言）与离屏渲染截图脚本，并用像素取样核对颜色 | 帮我抓到两处自己没注意的布局问题：底部提示文字被按钮压住、"剩余箭头"数字与标签挤成一团 | 重排底部信息栏（分隔线 → 提示文字 → 按钮）并留出底部边距；顶部改成先测量文字宽度再整体右对齐；核对完测试结论后才定稿 |

### 三次过程的细节还原（我的要求 → AI 产出 → 验证 → 我的改动）

**① 关卡设计：AI 反过来纠正了我的前提**

* **我提出的要求**：按上面这套规则帮我把关卡填好，并保证每一关都能通关。
* **AI 完成的事**：先指出这条规则本身允许**互锁死局** —— `[4, 0, 3]` 即 `→ _ ←`，两支箭头互相阻挡，怎么点都会撞，这一关永远清不掉；所以"随手填数字"是不行的。接着给出生成 + 校验脚本，并论证了贪心求解的**完备性**（消除只会让其它箭头的射线更通畅，属单调过程，"能拆就拆"永不亏，一旦卡住即为死锁）。
* **实际效果**：跑 `py -3.10 c:\Users\29303\Desktop\1.py --solve`，5 个关卡全部 `[OK] 可通关`，没有一关是死局。
* **我是否修改**：修改了。第 1 关换成我自己设计的 3×3 热身关；工作方式也从"让 AI 直接给关卡"改成"AI 生成候选、我来挑选"。

**② 路径检测：AI 一次写对，但我改了状态设计**

* **我提出的要求**：按文档 §4.1 实现 `blocker_of()`，返回"正前方到边界之间是否有箭头"。
* **AI 完成的事**：没有照抄四个方向的四段循环，而是收敛为一段 `while`，并把**挡路的那支箭头**返回出来。
* **实际效果**：一次通过；`--test` 中路径检测 6 项全过，另外用 `[[4, 0, 3]]` 专门确认互锁死局能被判为无解。
* **我是否修改**：修改了。把 `removed`（逻辑）和 `state`（表现）拆成两个字段——这是 AI 没主动做的，是我在检查动画流程时发现的设计缺陷。

**③ 动画：AI 给公式，我来调手感**

* **我提出的要求**：在 Pygame 里怎么做"平滑移动、抖动、淡出"。
* **AI 完成的事**：给出 `offset += 速度 × dt`、`sin(t × 62) × 衰减幅度` 的抖动、以及 `BLEND_RGBA_MULT` 淡出三套写法。
* **实际效果**：离屏取样确认被点箭头变红 `(255, 91, 91)`、被撞箭头闪金 `(255, 188, 91)`，飞行结束后状态为 `GONE` 且 `alpha = 0`。
* **我是否修改**：修改了。抖动幅度 10px → 6px、动画时长 → 0.40s、红闪 → 0.12s，并补上拖尾与被撞者闪金两处反馈。

### 关于记录真实性

以上过程**不是项目完成后补写的**：`1.py` 中至今保留着 AIGC 产出与我改动的对应痕迹，可逐一核对 ——

* `solve_level()` 函数与 `--solve` / `--test` 两个命令行入口（用于校验关卡可解性）。
* `Arrow.removed` 与 `Arrow.state` 两个字段及其注释（把「逻辑」与「表现」分开的设计）。
* `fit_font()` 的三级回退与 `CJK_FONT_FILES` 字体表（针对 pygame 字体缺陷的修复）。
* `draw_bottom()` 里的 `line_y`、按钮 y 坐标与 `draw_header()` 中的文字宽度测量（按自检结果重排的布局）。
* 相应的关键结论（23 / 23 项自测通过、5 关 0 死锁、像素颜色取样）本文档前四节均已留下原始记录。

**记录口径（避免歧义）**

* 本项目的代码与测试改动**都是在 AI 会话中完成的**，人的参与集中在"给出规则与文档、裁定方案方向、验收确认"三件事上（分工见第三节）。所以上表"人工修改"一列的写法是"**定稿前实际做过的改动**"，我没有把它包装成"我先写错、AI 再帮我修好"的故事。
* 第二节 Bug 表里第 4、5 项已明确标注为**写第一版时就预防性加上的防护**，不是事后发现的故障；真正的实测故障是前 3 项。
* 上表第 2、3 项属于"AI 一次通过、人工没改参数"的情况，也照实写，不硬凑成"调过参数"。
